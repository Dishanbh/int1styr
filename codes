#include <stdio.h>

int main()
{int x=4,y=3,z;
z=x-- -y;
    printf("\n%d%d%d",x,y,z);

    return 0;
}
#include<stdio.h>
int main()
{
    int i;
    int ch=1;
    for(i=1;i<=10000;i++)
    printf("%c",ch);
    return 0();
}
#include<stdio.h>
#include<conio.h>

void main()
{
    int a,b,c;
    printf("enter 2 no.s");
    scanf("%d%d",a,b);
    c=a+b;
    printf("sum is %d",c);
}
